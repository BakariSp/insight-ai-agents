# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.0

# LLM
litellm>=1.0
pydantic-ai>=1.0

# Data validation & settings
pydantic>=2.0
pydantic-settings>=2.0

# HTTP client
httpx>=0.27
requests>=2.31

# Tools
fastmcp>=2.0
numpy>=1.26

# Document generation & parsing
python-pptx>=1.0
python-docx>=1.0
openpyxl>=3.1

# RAG / Knowledge Base
# NOTE: lightrag-hku must be installed BEFORE raganything to avoid dep resolver backtracking.
# On Python 3.14+, use: pip install lightrag-hku && pip install raganything --no-deps
lightrag-hku>=1.4
raganything>=1.0
asyncpg>=0.30

# Redis
redis>=5.0

# Config
python-dotenv>=1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
